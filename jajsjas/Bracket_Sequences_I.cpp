#include <bits/stdc++.h>

using namespace std;

const long long N=1e6+3, md=1e9+7;

int main(){
    cin.tie(0); cout.tie(0); ios_base::sync_with_stdio(0);

    long long n;
    vector<long long> dp(N, 0);

    for(int i=2; i<N; i+=2){
        dp[i]=
    }

    return 0;
}